# AI 漫剧制作流水线骨架设计

> 本文件是「漫剧制作总控 Agent」的流水线蓝图。目标：让 maozh2 在**一个入口**里驱动整条生产流水线，每一步都能停下来调试/修改，一路跑到出片。
> 与 [TASK-SPEC.md](./TASK-SPEC.md)、[COLLABORATION.md](./COLLABORATION.md) 配套阅读。

---

## 0. 设计原则

- **人在回路（Human-in-the-loop）**：每一步产出后暂停，摆给 maozh2 看，确认或给修改意见后才进入下一步。绝不未经确认自动往下跑。
- **可调试**：每一步的输入 Prompt、参数、参考图都可见、可改、可重生。
- **可回退可复用**：每一步产出按版本归档；不满意可回退到上一个 checkpoint，不从头来。
- **可替换**：模型不写死，平台对接通过胶水代码/工作流模板，后续可换模型/换平台。
- **先跑通再系统化**：第一版不求全自动，先把带 checkpoint 的骨架跑通第一集，再逐步减少人工干预。

---

## 1. 7 步流水线（状态机）

每集从步骤 01 走到 07，每步是一个 checkpoint。

| 步骤 | 名称 | 输入 | 产出（checkpoint 内容） | 归档 |
|---|---|---|---|---|
| 01 | 剧本准备 | 剧本原文（英文/中文 DOCX）+ 集号 | 该集解说词（口播体、连贯、保留关键对白与情绪点） | `assets/<集号>/01-script/` |
| 02 | 分场分镜 | 01 解说词 | 分场表 + 分镜表（每句旁白对应人物/动作/场景/证据画面） | `assets/<集号>/02-storyboard/` |
| 03 | 一致性资产 | 02 分镜 + 角色清单 | 角色三视图 + 服装/表情状态 + 场景/道具图（基准资产） | `assets/<集号>/03-assets/` |
| 04 | 关键帧 | 02 分镜 + 03 资产 | 关键帧点位 + 关键帧图（引用一致性资产） | `assets/<集号>/04-keyframes/` |
| 05 | 分段视频 | 04 关键帧 | 4–15 秒分段视频 Prompt + 生成的视频片段（含首尾帧衔接） | `assets/<集号>/05-clips/` |
| 06 | 配音字幕 | 01 解说词 + 05 视频 | 旁白音频（TTS）+ 字幕轨（节奏对齐） | `assets/<集号>/06-voice-sub/` |
| 07 | 后期合成 | 05 视频 + 06 声音字幕 | 成片（剪辑/节奏/字幕/音乐/音效/调色/导出） | `assets/<集号>/07-final/` |

---

## 2. Checkpoint 交互协议（总控 ↔ maozh2）

每步跑完后，总控 Agent 发一个标准 checkpoint 消息：

```
【CHECKPOINT 步骤 0X · <名称>】集号：EP-01
产出：<文件/链接/预览>
关键参数/版本：v1 / <模型> / <关键 prompt>
请确认：
  ✅ 通过 → 进入下一步
  ✏️ 修改 → 告诉我改哪里（旁白/镜头/参数/参考图）
  ↩️ 回退 → 回到上一步重做
  🔄 重生 → 用新参数重跑本步
```

maozh2 回复后，总控才执行对应动作。

---

## 3. 版本归档规则

- 每步产出存到 `assets/<集号>/<步骤>/<版本>/`，如 `assets/EP-01/04-keyframes/v1/`。
- 每个版本目录含：`prompt.md`（Prompt）、`params.json`（参数/模型/seed）、`refs/`（参考图）、`output.*`（产出）、`meta.md`（说明/失败原因）。
- 最新通过版本标记为 `latest`（软链或 README 指针）；历史版本保留以供回退与复用。
- 跨集复用：03 一致性资产定稿后，作为后续集的基准资产入口。

---

## 4. 平台对接（通过 ComfyUI 平台集成工程师搭建）

| 步骤 | 平台 | 对接方式 |
|---|---|---|
| 01–02 | LLM（Claude/GPT） | API 调用，输出人工可复核的文本版本 |
| 03 资产 / 04 关键帧 | ComfyUI（核心编排） | ComfyUI API 提交工作流 → 轮询结果 → 下载归档 |
| 05 视频 | ComfyUI / Seedance / Kling / Wan | API 提交（含参考图/首尾帧）→ 回收视频 → 记录失败与重跑 |
| 06 配音 | ElevenLabs | TTS API（音色/语速/语言）→ 音频归档 |
| 07 后期 | 剪映 / PR / ffmpeg | 剪辑工程文件 + 导出参数归档 |

胶水代码统一提供：任务提交、参数记录、结果回收、失败重试、版本归档五个能力。

---

## 5. 失败重试机制

- 每步生成默认 N 次尝试（如 3 次），每次记录失败原因到 `meta.md`。
- 失败原因分类：内容不符（换 Prompt/参考图）、平台错误（重试/换模型）、超时（降参数/重试）。
- 连续失败超阈值 → 暂停并在 checkpoint 询问 maozh2 或求助 ComfyUI 工程师。

---

## 6. 角色与能力映射

| 能力 | 实现方式 |
|---|---|
| 流水线驱动 / checkpoint 管理 / 归档 | 漫剧制作总控 Agent（maozh2 直接驱动） |
| 剧本·分镜 | 总控调用的 skill / sub-agent |
| ComfyUI 工作流 + 平台胶水 + 骨架代码 | ComfyUI 平台集成工程师（builder） |
| 图像·视频生成执行 | 总控通过胶水代码调用平台完成 |
| 后期合成 | 总控调用的 skill / sub-agent 或人工（剪映/PR） |
| 导演 / PM / 最终确认 | maozh2（人） |

---

## 7. 真正开工前必须解锁

1. **剧本原文**：英文30集 / 中文30集 DOCX 放进仓库或项目资源。
2. **第1集确认**：确认做第1集（Serena 身份暴露起点）或指定别的集。
3. **平台账号**：ComfyUI 跑在哪台机器；视频模型 / ElevenLabs 是否有可用账号或 Key。

这三项解锁后，总控 Agent 从步骤 01 启动第1集。

---

## 8. 分期演进

- **Phase 1（跑通第1集）**：带 checkpoint 的骨架，逐步调试出第1集成片。
- **Phase 2（系统化）**：把第1集跑通的成功工作流/Prompt 锁成模板，减少人工干预，开始铺量。
- **Phase 3（可选）**：若要给非技术用户用，再考虑做前端面板（届时再招全栈）。
